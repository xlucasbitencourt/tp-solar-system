import React from 'react';

function Header() { // requisito 1
  return ( // o retorno é o formato html, entre () para poder ocupar várias linhas
    <header>
      <h1>Sistema Solar</h1>
    </header>

  );
}

export default Header; // exportação necessária para utilização no arquivo App.js
